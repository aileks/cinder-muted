# Cinder Muted for mpv

Copy `mpv.conf` to `~/.config/mpv/mpv.conf` and `modernz.conf` to `~/.config/mpv/script-opts/modernz.conf`.

`osc=no` hands the on-screen controller to [ModernZ](https://github.com/Samillion/ModernZ), a separate install.
