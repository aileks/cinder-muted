# Cinder Muted for rofi

## Install

Download `config.rasi` to `~/.config/rofi/config.rasi`, or `@import "cinder-muted.rasi";` from your own `config.rasi` after placing the file next to it. Rofi reads the config on each launch.
