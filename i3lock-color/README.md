# Cinder Muted for i3lock-color

A lock script running i3lock with the muted ring, fill, key highlight, and error colors.

## Install

Run `cinder-muted.sh` from your lock binding or idle hook, or copy its i3lock flags into your own lock script.
