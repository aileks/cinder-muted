# Cinder Muted for bat

## Install

Download `cinder-muted.tmTheme` to `$(bat --config-dir)/themes/`, rebuild the cache, and set the theme in `$(bat --config-dir)/config`:

```sh
bat cache --build
```

```ini
--theme="cinder-muted"
```
