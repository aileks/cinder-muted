# Cinder Muted for yazi

Two files: the UI theme and the preview syntax theme.

## Install

```sh
cp theme.toml cinder-muted.tmTheme ~/.config/yazi/
```

The included `theme.toml` already points `mgr.syntect_theme` at `~/.config/yazi/cinder-muted.tmTheme`.
