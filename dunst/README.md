# Cinder Muted for dunst

## Install

Download `dunstrc` to `~/.config/dunst/dunstrc`, or copy its color values into your own dunstrc. Restart dunst (`pkill -x dunst`; it respawns on the next notification).
